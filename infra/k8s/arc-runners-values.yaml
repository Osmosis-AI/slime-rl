# Helm values for the ARC runner scale set
# Deploy with:
#   helm install limes-gpu-runners \
#     --namespace arc-runners \
#     --create-namespace \
#     -f infra/k8s/arc-runners-values.yaml \
#     oci://ghcr.io/actions/actions-runner-controller-charts/gha-runner-scale-set

githubConfigUrl: "https://github.com/Osmosis-AI/slime-rl"
githubConfigSecret: arc-github-secret

# 1 node with 8 GPUs = 1 job at a time
maxRunners: 1
minRunners: 0

template:
  spec:
    restartPolicy: Never
    priorityClassName: ci

    # Required for Ray/NCCL multi-GPU communication
    hostIPC: true

    containers:
    - name: runner
      image: ghcr.io/osmosis-ai/slime-rl/arc-gpu-runner:latest
      command: ["/home/runner/run.sh"]
      env:
      - name: RUNNER_ALLOW_RUNASROOT
        value: "1"
      resources:
        limits:
          nvidia.com/gpu: "8"
      securityContext:
        privileged: true
      volumeMounts:
      - mountPath: /dev/shm
        name: dshm
      - mountPath: /root/models
        name: ci-storage

    volumes:
    - name: dshm
      emptyDir:
        medium: Memory
        sizeLimit: 16Gi
    - name: ci-storage
      persistentVolumeClaim:
        claimName: limes-ci-pvc
